meta {
  name: Get Client
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/fetch/common/clients
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "search":""
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
